**What this changes**

**Why**

**Checklist**
- [ ] `cargo test --features node --lib` passes with 0 failures
- [ ] `cargo check` is clean on default features too (fresh-clone build)
- [ ] Docs updated if behavior or defaults changed
- [ ] I license this contribution under the repo's terms (AGPL-3.0 for the
      node, MIT/Apache-2.0 for `crates/` and `sdks/` — see LICENSING.md)
