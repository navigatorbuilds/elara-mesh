\* Tightness, broken side (n=4, f=2 > 1/3, sealed).
\* Expected: NoAbortAndClaim VIOLATED (counterexample printed);
\* SealGateSound still holds.
CONSTANT Witnesses <- MCWit
CONSTANT Byzantine <- MCByz
CONSTANT Sealed    <- MCSealed
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT SealGateSound
INVARIANT NoAbortAndClaim
