\* LADDER / RecurLocalSealed. Expected: HOLDS — even with a periodic worst-case
\* epoch (slot 0 non-viable), re-randomization cycles an honest identity back
\* into an eligible rank, so the fast local path recurs (slots 1,2 each period).
CONSTANT BeaconPeriod = 3
CONSTANT ViableEpochs <- MCViable
CONSTANT EscalationAvailable = TRUE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT EscalImpliesSealed
INVARIANT LadderBeforeEscal
PROPERTY RecurLocalSealed
