\* NO-GLOBAL-GST / LiveWithEscalation. Expected: VIOLATED — all local
\* proposers Byzantine AND ExtGstReachable = FALSE: the committee is dead and
\* the escalation anchors can never deliver without global GST. The headline
\* no-floor result: in-zone liveness has NO quorum-free backstop, so global
\* asynchrony leaves the zone stuck indefinitely (FLP at the cross-zone layer).
CONSTANT Ranks              <- MCRanks
CONSTANT ByzRanks           <- MCByzRanks
CONSTANT Witnesses          <- MCWit
CONSTANT ByzWit             <- MCByzWit
CONSTANT ExternalAnchors    <- MCExt
CONSTANT ByzExt             <- MCByzExt
CONSTANT GstReachable        = TRUE
CONSTANT ExtGstReachable     = FALSE
CONSTANT EscalationAvailable = TRUE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT EscalImpliesSealed
INVARIANT LadderBeforeEscal
PROPERTY LiveWithEscalation
