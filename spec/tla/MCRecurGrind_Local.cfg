\* GRIND / RecurLocalSealed. Expected: VIOLATED — a grindable / adversary-pinned
\* beacon (no epoch viable) means the fast committee path never fires; the chain
\* seals ONLY via escalation forever. The lasso proves the chained VRF beacon's
\* re-randomization is load-bearing for staying on the fast local path.
CONSTANT BeaconPeriod = 3
CONSTANT ViableEpochs <- MCViable
CONSTANT EscalationAvailable = TRUE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT EscalImpliesSealed
INVARIANT LadderBeforeEscal
PROPERTY RecurLocalSealed
