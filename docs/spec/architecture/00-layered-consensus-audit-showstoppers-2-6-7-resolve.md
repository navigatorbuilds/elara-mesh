## Layered Consensus

Three layers replace both the testnet per-record model and the original binary epoch model — immediate per-record validation (Layer 1), epoch finalization (Layer 2), and post-epoch challenges (Layer 3), detailed in the sections that follow.
